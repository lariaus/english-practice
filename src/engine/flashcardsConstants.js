// Learning/Review batch size - see docs/flashcards-spec.md: "a fixed
// constant, 15 (changeable in code, not a user-facing setting)". Shared so
// the Learn/Review screens and any UI showing "how many this session" (e.g.
// the Edit screen's Learn button count) all agree on the same number.
export const BATCH_SIZE = 15
